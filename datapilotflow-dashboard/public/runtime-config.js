// Dev/default placeholder. In the published container image this file is
// regenerated at startup from the API_BASE_URL env var — see
// docker/services/dashboard/docker-entrypoint.sh. Leaving it empty here
// means local `npm run dev`/`vite build` falls back to VITE_API_BASE_URL.
window.__RUNTIME_CONFIG__ = {};
