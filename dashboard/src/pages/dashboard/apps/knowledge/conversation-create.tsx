/**
 * Conversation Creation - Wizard Mode Only
 * 
 * This component renders the conversation creation wizard.
 * The visual pipeline mode has been removed to simplify the user experience.
 */

import ConversationCreateWizard from './conversation-create-wizard';

/**
 * Main component - renders the conversation creation wizard.
 * Visual pipeline mode has been removed for simplicity.
 */
export default function ConversationCreate() {
  return <ConversationCreateWizard />;
}
